let morse = require.main.require("./config.morse.js");

class MorseDecoder {
	constructor() {
		this.morseCode = "";
	}

	addMorseSignal(morseSignal) {
		// Decode when needed
	}
}

module.exports = MorseDecoder;
